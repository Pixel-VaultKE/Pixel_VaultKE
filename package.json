{
  "name": "mother-engine-rules-tests",
  "description": "Attacks the tenant boundary from both sides against the real deployed Firestore rules, using the Firestore emulator",
  "scripts": {
    "test": "firebase emulators:exec --only firestore \"jest --runInBand\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "jest": "^29.7.0"
  },
  "private": true
}
